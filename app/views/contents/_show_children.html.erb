<div class="control-group">
  <%= f.label :title, :class => 'control-label' %>
  <div class="controls">
    <ol>
    <% show_children.each do |child| %>
      <%= content_tag :li, child.title %>
    <% end %>
    </ol>
  </div>
</div>
